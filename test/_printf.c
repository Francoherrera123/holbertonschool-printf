#include "printf.h"
/**
 *
 *
 *
 */
int _printf(const char *format, ...)
{
	int recor;

	for (recor = 0; format[recor]; recor++)
	{
		switch(format[recor + 1])
		case 37:




			/* _printf("hola %s\n %d\n", "mundo" 2 ); */
	}
}
